/** @type {import('next').NextConfig} */
const nextConfig = {
    // Ajoutez vos autres configurations ici si nécessaire
    output: 'standalone',
    basePath: '/gh-pages',
    assetPrefix: '/gh-pages/',
  };
  
  module.exports = nextConfig;
  